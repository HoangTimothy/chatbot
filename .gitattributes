* text=auto

# Internal documents should normally not be committed.
# If the policy changes later, move large files to Git LFS explicitly.
*.pdf filter=lfs diff=lfs merge=lfs -text
*.docx filter=lfs diff=lfs merge=lfs -text
*.xlsx filter=lfs diff=lfs merge=lfs -text

